DATABASE_URL=postgres://user:pass@localhost:5432/schulapp?sslmode=disable
JWT_SECRET=change-me-in-production
PORT=8080
UPLOAD_DIR=./uploads
# SchoolConnect-REST ("schoolconnect serve", v0.1.0). Lokal auf dem Host mit
# eigenem Port neben :8080 starten, aus Docker via host-gateway erreichbar.
SCHOOLCONNECT_BASE_URL=http://host.docker.internal:8081
